<!DOCTYPE html>
<html>
<head>
    <title>Products</title>
</head>
<body>
    <h1>Product List</h1>

    <% if (prods.length === 0) { %>
        <p>No products found</p>
    <% } %>

    <% prods.forEach(product => { %>
        <p><%= product.title %></p>
    <% }) %>
</body>
</html>